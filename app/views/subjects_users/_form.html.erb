
<%= simple_form_for(@subjects_user) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.hidden_field :subject_id, :value => params[:subject_id]  %>
  </div>
  
  <div class="form-inputs">
    <% @users.each do |u| %>
      
    <% end %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
